using ClassesAndObjects; // very similar to import in JS

// class instance (objects) are created using `new`
// the garbage collector recover the memory
Point p1 = new Point(0, 0);
Point p2 = new Point(10, 20);